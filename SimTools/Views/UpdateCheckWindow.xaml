<Window x:Class="SimTools.Views.UpdateCheckWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:controls="clr-namespace:SimTools.Controls"
        Title="SimTools — starting up…"
        WindowStartupLocation="CenterScreen"
        SizeToContent="WidthAndHeight"
        ResizeMode="CanResize"
        WindowStyle="None"
        ShowInTaskbar="True"
        ShowActivated="True"
        Background="{DynamicResource Surface}"
        Foreground="{DynamicResource OnSurface}">

    <!-- Custom caption via WindowChrome -->
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="8"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Dark palette -->
        <SolidColorBrush x:Key="Surface" Color="#121212"/>
        <SolidColorBrush x:Key="SurfaceCard" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="SurfaceBorder" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="OnSurface" Color="#EDEDED"/>
        <SolidColorBrush x:Key="OnSurfaceSecondary" Color="#B0B0B0"/>
        <SolidColorBrush x:Key="Accent" Color="#4F8EF7"/>
        <SolidColorBrush x:Key="ButtonBg" Color="#2E2E2E"/>
        <SolidColorBrush x:Key="ButtonBgHover" Color="#3A3A3A"/>
        <SolidColorBrush x:Key="ButtonBgPressed" Color="#252525"/>
        <SolidColorBrush x:Key="Separator" Color="#232323"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#FF6B6B"/>

        <!-- Card style -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceCard}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SurfaceBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <!-- Text styles -->
        <Style x:Key="TitleTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource OnSurface}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="BodyTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource OnSurfaceSecondary}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Buttons -->
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource OnSurface}"/>
            <Setter Property="Background" Value="{DynamicResource ButtonBg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SurfaceBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonBgHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonBgPressed}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource Accent}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Accent}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <!-- Progress -->
        <Style TargetType="ProgressBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Foreground" Value="{DynamicResource Accent}"/>
            <Setter Property="Background" Value="{DynamicResource Separator}"/>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Custom draggable title bar -->
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <controls:TitleBar Grid.Row="0" Grid.ColumnSpan="2"/>

        <Border Grid.Row="1" Style="{DynamicResource Card}" Margin="8">
            <StackPanel Orientation="Vertical" MinWidth="420">
                <TextBlock x:Name="TitleText"
                           Style="{DynamicResource TitleTextStyle}"
                           Text="Getting things ready…"
                           Margin="0,0,0,8"/>

                <Grid Margin="0,4,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ProgressBar x:Name="Bar" Grid.ColumnSpan="2" Minimum="0" Maximum="100" Value="0"/>
                    <TextBlock x:Name="PercentText"
                               Grid.Column="1"
                               Margin="8,0,0,0"
                               VerticalAlignment="Center"
                               Style="{DynamicResource BodyTextStyle}"
                               Text="0%"/>
                </Grid>

                <TextBlock x:Name="StatusText"
                           Style="{DynamicResource BodyTextStyle}"
                           Text="Checking for updates…"
                           Margin="0,10,0,0"/>

                <TextBlock x:Name="ErrorText"
                           Foreground="{DynamicResource ErrorBrush}"
                           Visibility="Collapsed"
                           Text="We couldn’t reach the update server. Please check your connection."
                           TextWrapping="Wrap"
                           Margin="0,10,0,0"/>

                <DockPanel Margin="0,14,0,0" LastChildFill="False">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="CloseBtn"
                                Content="Close"
                                Click="CloseBtn_Click"
                                Visibility="Collapsed"/>
                        <Button x:Name="UpdateBtn"
                                Content="Install update"
                                Click="UpdateBtn_Click"
                                Style="{DynamicResource PrimaryButton}"
                                Visibility="Collapsed"
                                Margin="8,0,0,0"/>
                    </StackPanel>
                </DockPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
