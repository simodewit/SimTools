<Window x:Class="SimTools.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="#00000000"
        ShowInTaskbar="False" Topmost="True" Width="960" Height="540"
        Title="Overlay">
    <Grid>
        <Canvas>
            <ItemsControl ItemsSource="{Binding OverlayElements}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <!-- Untyped template -->
                    <DataTemplate>
                        <Border Background="#CC0D0F13" CornerRadius="8" Padding="8"
                                Canvas.Left="{Binding X}" Canvas.Top="{Binding Y}">
                            <StackPanel>
                                <TextBlock Text="Mapping" Foreground="White" Opacity="0.85" FontSize="12"/>
                                <TextBlock Text="{Binding DataContext.State.CurrentMap.Name, RelativeSource={RelativeSource AncestorType=Window}}"
                                           FontSize="20" FontWeight="Bold" Foreground="White"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Canvas>
    </Grid>
</Window>
