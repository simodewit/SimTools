<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- App-wide overlay resources (DynamicResource everywhere) -->
    <SolidColorBrush x:Key="Overlay.PanelBrush" Color="#1D2024"/>
    <SolidColorBrush x:Key="Overlay.BorderBrush" Color="{DynamicResource Color.Border}"/>
    <FontFamily x:Key="Overlay.FontFamily">Segoe UI</FontFamily>

    <Style x:Key="OverlayText.Heading" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource Overlay.FontFamily}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextDim}"/>
    </Style>

    <Style x:Key="OverlayText.Title" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource Overlay.FontFamily}"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text}"/>
    </Style>

    <Style x:Key="OverlayText.Label" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource Overlay.FontFamily}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextDim}"/>
    </Style>

    <Style x:Key="OverlayText.Body" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource Overlay.FontFamily}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text}"/>
    </Style>

    <Style x:Key="OverlayPanel.Container" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Overlay.PanelBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.OverlayBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

</ResourceDictionary>
